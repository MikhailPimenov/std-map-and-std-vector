#ifndef TYPEDEFS_H
#define TYPEDEFS_H

typedef int element_t;                      // Корректно работает, если целый тип .
typedef unsigned int length_t;              // Корректно работает, если целый тип .
typedef length_t key_t;                     // Корректно работает, если целый тип .

#endif // TYPEDEFS_H
